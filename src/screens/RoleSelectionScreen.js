import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const RoleSelectionScreen = () => {
  return (
    <View>
      <Text>RoleSelectionScreen</Text>
    </View>
  );
};

export default RoleSelectionScreen;

const styles = StyleSheet.create({});
